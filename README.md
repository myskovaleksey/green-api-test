# Автотесты для MAX (GREEN-API)

Набор автоматизированных тестов для проверки API и UI функционала сервиса GREEN-API. 
Проект демонстрирует навыки тестирования REST API для 
  - методов sendMessage и getChatHisstor.
  - проверяют статус код 200 и 400
  - проверяют обязательные поля
  - проверяют что инстанс авторизован с помощью метода getStateInstance


## 📋 Реализованные проверки

### 1. API Тестирование (Jest + Axios)
- **getStateInstance**: Проверка жизненного цикла инстанса (статус `authorized`).
- **sendMessage**: 
  - Отправка текста и Emoji.
  - Тестирование превью ссылок (`linkPreview`).
  - Валидация ограничений (длина сообщения, корректность параметров `typingTime`).
  - Проверка статус-кодов 200, 400, 429, 500.
- **getChatHistory**: Получение истории сообщений по числовому ID чата (`numericChatId`).

### 2. UI Тестирование (Selenium WebDriver)
- **Smoke Tests**: Проверка доступности веб-интерфейса.
- **Page Object Model**: Использование паттерна для разделения логики тестов и элементов страницы.

## 🛠 Технологии
- **TypeScript** & **Node.js**
- **Jest** (Test Runner)
- **Selenium WebDriver** (UI Automation)
- **Axios** (HTTP Client)

## ⚙️ Настройка и запуск

1. **Установка зависимостей:**
   ```bash
   npm install
   ```
2. **Конфигурация окружения:**
Создайте файл .env в корне проекта и заполните его данными вашего инстанса:

```
   API_URL=qpi url из панели
   MEDIA_URL=qpi url из панели
   ID_INSTANCE=ваш_id_инстанса
   API_TOKEN_INSTANCE=ваш_токен_из_панели
   RECIPIENT_PHONE=79999999999@c.us # JID для отправки сообщений
   NUMERIC_CHAT_ID=0000000          # ID для работы с историей чата
```

3. **Запуск тестов:**

- **Запустить все тесты:**
    ```bash
      npx jest
    ```
- **Запустить только API тесты:**
    ```bash
      npx jest src/tests/methods/
    ```
- **Запустить только конкретный API тест:**
    ```bash
      npx jest src/tests/methods/stateInstance.test.ts
    ```
- **Запустить только конкретный API тест:**
    ```bash
      npx jest src/tests/selenium/
    ```
    
3. **Особенности реализации**

- **Стабильность (Throttling):** API-тесты включают управляемые задержки (sleep), чтобы избежать превышения лимитов запросов к серверам WhatsApp.

- **Безопасность:** Тесты проверяют поведение системы при использовании невалидных токенов и некорректных форматов данных.